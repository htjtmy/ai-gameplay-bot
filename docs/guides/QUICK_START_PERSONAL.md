# 🎮 AI 游戏机器人 - 快速参考

## 🚀 最快开始方式

```bash
# 一条命令完成所有：录制 → 处理 → 训练 → 完成
python scripts/quick_start_training.py all
```

按 **Q** 停止录制，脚本会自动处理数据和训练模型。

---

## 📋 分步方式

### 1️⃣ 录制你的游戏

```bash
python scripts/quick_start_training.py record
```

**参数：**
- `--session my_game` - 自定义会话名称
- `--screen X Y WIDTH HEIGHT` - 设置屏幕区域（例：`0 0 1920 1080`）

**按 Q 停止录制**

---

### 2️⃣ 处理录制数据

```bash
# 自动使用最新的录制
python scripts/quick_start_training.py process --latest

# 或指定特定会话
python scripts/quick_start_training.py process --session data/raw/gameplay_videos/20250125_120000
```

输出：
- ✅ 提取的视频帧
- ✅ 操作标注
- ✅ 训练数据集 (CSV)

---

### 3️⃣ 训练模型

```bash
python scripts/quick_start_training.py train
```

或直接运行：
```bash
python models/transformer/transformer_training.py
```

---

### 4️⃣ 部署服务

```bash
python scripts/quick_start_training.py deploy
```

访问：`http://localhost:8000`

---

## 🎯 常用命令

| 命令 | 说明 |
|------|------|
| `python scripts/quick_start_training.py all` | 完整流程（推荐） |
| `python scripts/quick_start_training.py record` | 仅录制 |
| `python scripts/quick_start_training.py process --latest` | 仅处理最新录制 |
| `python scripts/quick_start_training.py train` | 仅训练 |
| `python scripts/quick_start_training.py deploy` | 仅部署 |

---

## 📁 数据流向

```
你的游戏操作
    ↓
data/raw/gameplay_videos/
├── 20250125_120000/
│   ├── gameplay.mp4          ← 你的录制视频
│   ├── inputs.jsonl          ← 记录的键盘鼠标操作
│   └── metadata.json         ← 元数据
    ↓
data/processed/
├── frames_20250125_120000/   ← 提取的视频帧
├── actions_20250125_120000.txt ← 操作标注
└── dataset_20250125_120000.csv ← 训练数据集
    ↓
models/transformer/
└── weights/transformer_best.pth ← 训练好的模型
```

---

## 🎮 支持的游戏操作

### 键盘

| 按键 | 动作 |
|------|------|
| W | 前移 |
| A/S/D | 左/后/右移 |
| Q/E | 左/右转向 |
| Space | 跳跃 |
| C | 战斗技能 |
| X | 终极技能 |
| R | 重载 |
| F | 交互 |
| I | 背包 |
| M | 地图 |
| P | 菜单 |
| Shift | 闪避 |
| Ctrl | 滑行 |

### 鼠标

| 操作 | 动作 |
|------|------|
| 左键点击 | 近战攻击 |
| 右键点击 | 远程攻击 |
| 鼠标移动 | 视角转向 |

---

## 💡 提示

### 增加数据量
- 多录制几个会话
- 结合 `scripts/generative_ai_enrichment.py` 数据增强

### 加快训练
```bash
# 跳帧处理（每5帧取1帧）
python scripts/quick_start_training.py process --latest --skip 5
```

### 自定义屏幕区域
```bash
# 例：1920x1080 游戏窗口
python scripts/quick_start_training.py record --screen 0 0 1920 1080
```

### 查看帮助
```bash
python scripts/quick_start_training.py --help
python scripts/gameplay_recorder.py --help
python scripts/process_gameplay_recording.py --help
```

---

## 📊 工作流程总结

```
┌─────────────────────────────────────┐
│ 1. 录制游戏 (按 Q 停止)             │
│    python quick_start_training.py   │
│    record                           │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│ 2. 处理数据                         │
│    提取帧、映射操作、生成数据集      │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│ 3. 训练模型                         │
│    Transformer + 27 个游戏动作      │
└────────────┬────────────────────────┘
             ↓
┌─────────────────────────────────────┐
│ 4. 部署服务                         │
│    本地运行 AI 控制器               │
│    http://localhost:8000            │
└─────────────────────────────────────┘
```

---

## 🆘 常见问题

**Q：录制时屏幕黑屏？**  
A：调整屏幕区域参数：`--screen 0 0 1920 1080`

**Q：模型训练很慢？**  
A：使用 `--skip 2` 或 `--skip 5` 跳帧加速

**Q：操作没有被记录？**  
A：确保窗口不在最前面，检查 `inputs.jsonl` 是否有内容

**Q：如何修改支持的游戏动作？**  
A：编辑 `scripts/process_gameplay_recording.py` 中的 `INPUT_TO_ACTION_MAP`

---

## 📚 更多信息

详见完整指南：[PERSONAL_TRAINING_GUIDE.md](PERSONAL_TRAINING_GUIDE.md)

---

**祝你训练成功！** 🎯
