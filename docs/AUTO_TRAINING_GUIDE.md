# 自动化训练使用指南

## 🚀 快速开始

### 方法1: 双击启动（最简单）
直接双击 `start_auto_training.bat` 文件

### 方法2: PowerShell启动
```powershell
.\start_auto_training.ps1
```

### 方法3: 命令行启动
```bash
conda activate Ai-Gameplay-Bot
python scripts/auto_train_pipeline.py
```

---

## 📋 流水线执行步骤

脚本会自动按顺序执行以下步骤：

### 1. 数据增强（15-30分钟）
- 为动作4（近战攻击）生成约927个增强样本
- 为动作5（远程攻击）生成约851个增强样本
- 使用旋转、亮度、对比度、噪声、翻转等技术
- **目标**: 将少数类别样本数提升到1000个

### 2. 模型训练（2-4小时）
- 使用增强后的数据集
- **启用类别权重**: 自动平衡各类别的损失贡献
- **训练参数**:
  - Epochs: 100（最多）
  - Early Stopping: 15（连续15个epoch无改善则停止）
  - Batch Size: 16
  - Learning Rate: 0.0001
  - Optimizer: AdamW with weight decay
  - Scheduler: CosineAnnealingWarmRestarts

### 3. 模型评估（5分钟）
- 在测试集（1000个样本）上完整评估
- 生成每个动作类别的准确率
- **关注指标**:
  - 动作0（无动作）: 应保持>95%
  - 动作4（近战攻击）: 目标>60%
  - 动作5（远程攻击）: 目标>60%

### 4. 模型备份（1分钟）
- 自动备份训练好的模型
- 文件名格式: `transformer_model_backup_YYYYMMDD_HHMMSS.pth`

---

## 📊 监控和日志

### 日志文件位置
```
logs/
  ├── auto_train_YYYYMMDD_HHMMSS.log          # 详细执行日志
  └── auto_train_report_YYYYMMDD_HHMMSS.json  # JSON格式报告
```

### 实时监控

**方法1: 查看日志文件**
```powershell
# 实时追踪日志（在另一个终端）
Get-Content -Path "logs\auto_train_*.log" -Wait -Tail 50
```

**方法2: 检查训练进度**
查看 `models/transformer/training_history.json` 文件

---

## ⏱️ 预计时间

| 步骤 | 预计时间 | 超时限制 |
|------|----------|----------|
| 数据增强 | 15-30分钟 | 1小时 |
| 模型训练 | 2-4小时 | 8小时 |
| 模型评估 | 3-5分钟 | 10分钟 |
| 模型备份 | <1分钟 | 1分钟 |
| **总计** | **3-5小时** | **最多9小时** |

> 💡 实际时间取决于你的硬件配置（CPU/GPU）

---

## ✅ 成功标准

训练完成后，检查以下指标：

### 最低可接受标准
- [x] 动作0准确率 >95%
- [x] 动作4准确率 >50%
- [x] 动作5准确率 >50%
- [x] 总体准确率 >85%

### 良好标准
- [x] 动作0准确率 >98%
- [x] 动作4准确率 >65%
- [x] 动作5准确率 >65%
- [x] 总体准确率 >90%

### 优秀标准
- [x] 所有动作准确率 >80%
- [x] 总体准确率 >95%

---

## 🔍 检查结果

### 1. 查看训练报告
```bash
# 打开最新的JSON报告
cat logs/auto_train_report_*.json | Select-Object -Last 1
```

### 2. 查看训练历史
```bash
cat models/transformer/training_history.json
```

### 3. 手动测试模型
```bash
python scripts/test_model.py \
  --model "models/transformer/transformer_model.pth" \
  --dataset "data/processed/transformer_dataset_test.csv" \
  --full-eval
```

---

## 🎮 部署测试

训练完成后，在真实游戏中测试：

```bash
python scripts/real_time_controller.py \
  --model "models/transformer/transformer_model.pth" \
  --fps 10 \
  --confidence 0.6 \
  --duration 300
```

**观察指标**:
- ✅ 是否执行了战斗动作（不是只有动作0）
- ✅ 战斗动作是否在合适的时机执行
- ✅ 是否有误判（该攻击时不攻击，不该攻击时攻击）

---

## ⚠️ 故障排除

### 问题1: 数据增强失败
**可能原因**: 找不到原始数据集
**解决方法**: 
```bash
# 检查数据集是否存在
ls data/processed/transformer_dataset.csv
```

### 问题2: 训练过程中显存不足
**解决方法**: 编辑 `scripts/auto_train_pipeline.py`，修改 batch-size:
```python
'--batch-size 8 '  # 从16改为8
```

### 问题3: 训练时间过长
**解决方法**: 减少epochs或调整early stopping:
```python
'--epochs 50 '  # 从100改为50
'--early-stopping 10 '  # 从15改为10
```

### 问题4: 流水线中断
**恢复方法**: 
- 查看日志找到失败的步骤
- 手动执行该步骤
- 继续后续步骤

---

## 🔄 重新训练

如果对结果不满意，可以调整参数重新训练：

### 场景1: 战斗动作准确率仍然很低（<50%）
**建议**: 
1. 增加数据增强的目标样本数（2000-3000）
2. 录制更多真实战斗场景
3. 调整类别权重（手动设置更高权重）

### 场景2: 训练过拟合（训练准确率高但验证准确率低）
**建议**:
1. 减少epochs
2. 增加early stopping patience
3. 增加dropout率

### 场景3: 训练准确率和验证准确率都很低
**建议**:
1. 增加训练epochs
2. 调整学习率（尝试0.001或0.00005）
3. 检查数据质量

---

## 📞 需要帮助？

如果遇到问题：

1. **查看日志**: `logs/auto_train_*.log`
2. **检查报告**: `logs/auto_train_report_*.json`
3. **查看训练历史**: `models/transformer/training_history.json`
4. **手动测试**: 运行单个步骤，观察错误信息

---

## 🎯 下一步

训练完成后：

1. ✅ 查看评估结果
2. ✅ 在游戏中测试
3. ✅ 根据实际表现决定是否需要：
   - 录制更多数据
   - 调整参数重新训练
   - 直接部署使用

---

**祝训练顺利！🚀**
